#include "./Logger.oj"
#include "./Logger.oj"

#ifdef PRODUCTION
var foo = "bar";
#else
var foo = "baz";
#endif

@implementation App
-(id)init {
    if(self = [super init]) {
        [Logger info:"Hello, world!"];
    }
}
@end

function Main() {
    var app = [[alloc] init];
}
